[package]
name = "archivist-client-core"
description = "Archivist client cursors, crash-safe spool, upload scheduling, and acknowledgement state machine"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
archivist-adapter-sdk = { path = "../archivist-adapter-sdk" }
archivist-auth = { path = "../archivist-auth" }
archivist-protocol = { path = "../archivist-protocol" }
rusqlite.workspace = true
# Filesystem free-space probing for the spool pressure floor (plan
# Section 7.9); the rationale for the wrapper choice lives with the
# workspace dependency's entry.
rustix.workspace = true

[lints]
workspace = true
