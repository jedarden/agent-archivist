[package]
name = "archivist-storage"
description = "Archivist storage capability model and the raw-write, control-read, control-admin, and audit/restore store traits"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
archivist-protocol = { path = "../archivist-protocol" }
# The `zstd-v1` codec primitive (`crate::zstd_v1`) behind the blob commit
# path's `BlobEncoder` seam. The workspace pin carries the feature rationale
# and the profile-lifetime version rule.
zstd-safe = { workspace = true }

[lints]
workspace = true
